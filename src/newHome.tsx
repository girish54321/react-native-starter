import React, { FC } from "react";
import { View } from "react-native";
import { Headline } from "react-native-paper";

export const Home = () => {
    return (
        <View style={{ flex: 1 }}>
            <Headline>Home Name</Headline>
        </View>
    )
}

